part of "home_screen.dart";

class ContentAlarmScreen extends StatelessWidget {
  const ContentAlarmScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(color: AppColor.lightPurple),
    );
  }
}
