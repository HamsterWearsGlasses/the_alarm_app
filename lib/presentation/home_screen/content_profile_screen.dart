part of "home_screen.dart";

class ContentProfileScreen extends StatelessWidget {
  const ContentProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(color: AppColor.textColor),
    );
  }
}
